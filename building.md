# Cathedralis Magna Montis：Minecraft 山地哥特大教堂建造规范

> 交给 DeepSeek Agent 的长期施工文件。项目运行于 README 所述的本机 Minecraft Java 独立服务器；玩家和 `DeepSeekBot` 同场协作。本文的坐标、功能请求和进度状态均须以游戏世界和实际工具返回为准。

## 0. 先向 DeepSeek 声明：补足建造能力与游玩便利性

**DeepSeek，请先读本节和项目 README，清点当前真实可用的工具，并报告缺口。** README 当前列出的 19 个 MCP 工具中，建造只有单块 `mc_place`、拆除只有 `mc_dig`，扫描主要是按方块类型与半径检索；原设计明确没有给 Agent 任意服务端命令权限。单块施工难以可靠完成长约 274 格、双塔高约 180 格的工程。请在获得项目维护者授权并完成代码实现及测试后，优先补足下列受控能力；**不能将本文件写出的工具名当成已可调用的工具**。

| 优先级 | 要增加或调整的功能 | 建议的受控接口与验收条件 |
| --- | --- | --- |
| P0 | 批量施工 `mc_batch_place` | 一次提交约 100–1000 个 `(x,y,z,block,block_state)`；逐项校验坐标、方块名、允许区域和数量；输出成功、失败及失败位置；幂等重试，限制速率。 |
| P0 | 区域操作 `mc_fill` / `mc_replace` | 长方体填充、空气挖掘、材料替换均限制体积和总批量；先预览操作范围、预计改动数，确认后执行；保护玩家周围、既有建筑和禁区。 |
| P0 | 蓝图与断点 `mc_blueprint` | 导入或生成按阶段、开间分块的建筑蓝图；预览差异，只修缺失或错误方块；记录每个批次的世界实际完成状态，失败可续跑。蓝图格式和导入方式由项目实现决定。 |
| P0 | 区域实测 `mc_scan_region` | 返回限定范围内方块、空气、地形高度、碰撞和可达性，支持分页与采样；可复核柱网、镜像和门洞。现有 `mc_scan` 不能被假定具有完整区域快照能力。 |
| P0 | 回滚与审计 | 每批记录修改前后方块及执行者；支持按批撤销/恢复，限制跨阶段回滚；服务器审计日志能够核对已写入的方块。 |
| P1 | 复制/镜像 `mc_clone` | 仅在目标范围为空或有明确覆盖许可时复制；先检查镜像轴、方向方块状态、容器与实体；北南结构可按 `z=0` 对称生成。 |
| P1 | 长任务队列与进度 | 分开“提交”“进度”“暂停”“恢复”“取消”；分批执行并按区块加载和限速调度；游戏内 `/msg` 回复任务编号与简短进度，防止单轮超时或 900 字回复截断造成误判。 |
| P1 | 玩家协作便利 | 可按玩家位置设定建造原点；一键导航至阶段入口，临时脚手架与维修通道有明确拆除计划；材料需求清单、储物箱补给与交接报告。保留现有 `mc_follow`、`mc_goto_player`、`mc_give`。 |
| P2 | 可选择的施工模式 | 为专用建筑世界提供**受控的**创造模式或专用材料补给、昼夜/天气设置和安全传送；玩家可选择自由游玩或生存施工。应由服务端受控接口或人操作，不意味着给 Agent 开放任意 `/command`、`/op`、RCON 或外网监听。 |

**需要调整的限制**：当前 Agent 的 DSH profile 为 `read-only` 且 Minecraft MCP 不提供任意命令执行。若由 Codex/维护者升级项目，仅给受控建筑接口最小必需权限；不要直接把文件系统写权限、服务器控制台或重置世界权限交给游戏内聊天。现有 `run-server.sh cmd` 是人工运维入口，不能等同 Agent 的 MCP 能力。单条游戏内任务默认队列、超时和回复长度见 README；长工程应改为可恢复的后台任务。daemon 与一次性 `run-agent.sh` 使用同名机器人，不要同时启动。

**开工门槛**：先报告“已实现并验证 / 尚未实现”的能力清单；如果能力未升级，仍可只用现有工具施工一个可实际核验的小模块，并如实记录进度，不得虚构 `fill`、`clone` 或已建结构。整个工程只动此专用服务器世界，不重置、不覆盖既有作品，除非玩家另行明确指定。

## 1. 项目目标与坐标契约

建造一座位于山地台地上的晚期哥特风格大教堂：西侧双塔、中殿、十字耳堂、唱诗班与主祭坛、东端多边形后殿、环廊和七座放射礼拜堂；配备地下墓室、飞扶壁、彩窗、管风琴以及外围广场、修道院、主教宫和墓园。所有主要空间应真实可进入，玩家可在地面、地下、塔楼和屋顶维护通道间通行。

- 世界原点 `O=(X0,Y0,Z0)`：西立面中央主门地面中心，由 PHASE 0 实测后锁定。
- 本文 `x,y,z` 均为**相对坐标**；写入世界时 `X=X0+x`、`Y=Y0+y`、`Z=Z0+z`。约定 `x` 增大指向教堂东端、`z<0` 为北、`z>0` 为南；若实际世界方位不符合，先记录旋转变换，不可把相对坐标直接当世界坐标。
- 中轴 `z=0`；主体从 `x=0` 至 `x=273`，地坪 `y=0`；北南主要结构关于 `z=0` 镜像。地宫约 `y=-17..-3`，双塔最高约 `y=180`。测量世界可建上下界后，留至少 8 格顶部余量，确认 `Y0+180` 可建且地下范围不越界。
- 坐标是**设计控制值**，山地地基、门洞、扶壁或周边道路需要细化时记录变更。主体轴线、开间模数、双塔高度和中央通视必须保持一致。

### 1.1 平面分区

| 区域 | 相对范围 | 核心内容 |
| --- | --- | --- |
| A 西立面 | `x=0..42` | 三门、前厅、双塔、玫瑰窗 |
| B 中殿 | `x=43..154` | 八个 14 格开间、中央中殿与南北侧廊 |
| C 耳堂/十字交叉部 | `x=155..189`，横向至 `z=±78` | 南北耳堂、大型花窗 |
| D 唱诗班/圣坛 | `x=190..231` | 唱诗班席、主祭坛 |
| E 东端 | `x=232..273` | 多边形后殿、环廊、七座放射礼拜堂 |
| F 地下墓室 | 约 `x=75..244, z=-35..35, y=-17..-3` | 结构化柱网与通道 |
| G–K 外围 | 建筑外缘 | 飞扶壁、西广场、北修道院、南主教宫、东墓园 |

中殿柱线在 `x=43,57,71,85,99,113,127,141,155`；因此飞扶壁、拱肋、窗的节奏均按 14 格模数布置。**尺寸冲突须在 PHASE 1 冻结前解决**：原稿同时写“中央中殿净宽 29 格”和主柱中心 `z=±15`、束柱宽 5–7 格；两者无法同时成立。推荐把主柱中心移至 `z=±18`，即使束柱宽达 7 格，也能留下至少 `z=-14..14` 共 29 格的中央净通视带；侧廊柱线相应调整为 `z=±31、±44`，外围墙体约 `z=±52`，扶壁基础约 `z=±56` 起。所有平面、拱顶和窗位统一采用最终冻结柱网。若玩家更看重原稿 `±15/±28/±42` 柱线，则先实测并接受较窄的净宽，绝不在建造中途混用两套坐标。

西塔中心约 `z=-34`、南塔中心约 `z=+34`。**27×27** 指双塔主要塔身轮廓，约覆盖 `z=-47..-21` 和 `z=+21..+47`；原稿 `z=±18..±51` 可用作装饰、外凸墙段和门廊的最大轮廓，避免把“塔身 27”与“立面分区 34”误当同一尺寸。

## 2. 山地、建筑语言与材料

### 山地与台地

- 勘察山脊或宽阔山坡：东向有至少约 300 格可布置主体及东端，南北有至少约 180 格空间；西侧可远眺双塔。
- 尽量保留地形，设三层台地：城市入口约 `Y0-12`、大教堂广场 `Y0`、东侧修道院/墓园按地形约 `Y0-4..Y0+6`。山坡悬空处建实际石质基础，不允许主体漂浮。
- 西广场约宽 170 格、进深 110 格，中央石路对齐 `z=0`，两侧喷泉、雕像、花坛与阶梯；高树不要挡住西立面。北修道院含回廊、庭院、宿舍、图书室、食堂和小礼拜堂；南主教宫含庭院、接待厅、小礼拜堂和花园；东墓园含石墙、小径与墓碑。外围房屋约 25–35 格高，不抢双塔轮廓。

### 中殿与结构

- 主柱应为 5×5 至 7×7 视觉尺度的束柱：3×3 核心、四向附柱、三级柱基和明显柱头，禁止仅用 2×2 直柱接屋顶。中央视线从西门至祭坛保持畅通；楼梯和柱子不横跨中心轴。
- 垂直剖面（相对地坪）：柱基 `y=0..8`、侧廊拱顶约 `y=35`、主柱顶 `y=42`、主拱起拱 `y=44`、高窗约 `y=48..61`、肋拱最高 `y=68`、屋架约 `y=72`、主屋脊 `y=86`。用现场剖面校核，不把“净宽”和“柱中心距”混同。
- 每个标准矩形开间由四角主柱、两条对角肋、横肋和纵肋形成真实上升的肋拱；肋材相对填充突出至少 1 格。不得用平顶下贴假拱。每个主开间外侧对应一组飞扶壁：内墙承点、弧形飞券、外扶壁塔、尖塔、排水雕饰；扶壁塔约 45–54 格高，飞券连接约 `y=40..57`，位置对齐内部柱网和窗口。

### 西立面与双塔

- 西立面整体约宽 109 格，严格以 `z=0` 镜像，包含三座层层内退的尖券门、约 23 格视觉直径的玫瑰窗、雕像长廊、钟楼、开放石窗格和双尖塔。
- 中门控制范围 `z=-8..8`、高约 28 格；副门各约 11 格宽、高约 22 格（北约 `z=-38..-28`，南镜像约 `z=+28..+38`）。原稿北副门 `-38..-27` 与南副门 `+27..+38` 的端点宽度按含端点计为 12 格；施工图应统一为真正的 11 格净洞宽，并单独标门框。四层以上递进尖券和山花必须在设计图上区分结构与门洞。
- 玫瑰窗中心约 `(x=3..5, y=43, z=0)`，外半径约 11 格，设置中心石核、半径约 4/8 的分环及放射状石窗格；窗内用蓝、红、紫组合。
- 两塔同步分层施工：`y=0..31` 基础门户；`32..58` 尖券窗；`59..94` 钟楼；`95..118` 开放石廊；`119..137` 方形到八角过渡；`138..160` 镂空尖塔；`161..176` 细尖顶；`177..180` 顶饰。塔顶采用开放式框架、削角八角、垂直肋和角部小尖塔，不做逐层缩小的实心金字塔。主塔需有可通行的楼梯、钟楼平台、观察层与维修通道。

### 东端、内部和采光

- 主祭坛约 `x=216..224, z=-7..7`，高出地坪 3–5 格，设祭台、烛台、高背屏与十字架；西门中央能望见其中心视觉焦点。
- 东端建立多边形后殿、可通行环廊和七座可进入的放射礼拜堂（北三、中央一、南三）。北侧彩窗偏深蓝、蓝紫、紫白；中央金白蓝；南侧红紫、深红、金红。北耳堂蓝青淡蓝，南耳堂红橙黄；圣坛金白浅蓝。所有彩窗要有石窗格与尖券轮廓，不能只是矩形彩色玻璃。
- 西端中殿上方约 `x=28..41, y=23..40` 建可到达的管风琴平台：琴柜、不同长度管道、键盘台、栏杆和风箱。另配忏悔室、圣器室、唱诗班席、长椅和维修通道。
- 地下墓室约 `x=75..244,z=-35..35,y=-17..-3`，柱网呼应上层，先结构后墓龛、石棺和小祭坛；至少两条实际连通地面的楼梯。

### 材料规范

| 用途 | 主要材料 |
| --- | --- |
| 主结构 | `stone_bricks`；局部 `andesite`、`polished_andesite` |
| 肋拱与雕刻高光 | `polished_andesite`、`stone_bricks`、少量 `calcite` |
| 墙体老化与阴影 | 少量 `cracked_stone_bricks`、`mossy_stone_bricks`；`tuff_bricks`、`polished_tuff`、`deepslate_bricks` |
| 屋面与木作 | `deepslate_tiles`（若该版本实际无此方块，先映射到可用的深板岩楼梯/台阶等）、`polished_deepslate`；`dark_oak`、`spruce` |
| 金属与照明 | `iron_bars`、`chains`、`lantern`、少量 `soul_lantern`；避免大量裸露萤石和火把 |

大面积石墙的参考比例约为 55% 石砖、15% 安山岩、10% 磨制安山岩、8% 凝灰岩砖、5% 裂纹石砖、3% 方解石、2% 苔石砖、2% 其他；**仅用于墙面视觉分布**，柱、拱肋、窗框和承重点必须有固定结构规律，不可随机噪声化。施工前核对 Minecraft 实际方块 ID 与版本，不认识的名称不得直接下发。

## 3. 分阶段施工路线

每阶段先做“测量 → 定位 → 骨架 → 墙体 → 柱 → 拱 → 屋顶 → 扶壁 → 窗 → 内部 → 雕刻 → 灯光 → 家具 → 检查”中对应的步骤。完成一个模块后立刻核对坐标、尺寸、中心线、镜像与通行；发现基础错误先返修。

| 阶段 | 本阶段可交付结果 | 停止点 |
| --- | --- | --- |
| 0 勘察 | 选原点 `(X0,Y0,Z0)`、测世界高度、地形和可扩建范围 | 仅报告，不建主体 |
| 1 测量 | 定 `x=0,43,155,190,232,273`；中轴、最终柱线、外墙、扶壁、耳堂端线临时标记 | 冻结统一柱网和旋转变换 |
| 2 地基 | 西塔、中殿、耳堂、唱诗班、东端地基，深约 5–9 格并支撑山坡 | 无悬空结构 |
| 3 地宫 | `x=75..244` 柱网、拱、两条楼梯和通道 | 先不做大量墓饰 |
| 4 西立面下部 | `x=0..42, y=0..32` 三门、双塔基、西厅 | 同步建两塔 |
| 5 中殿柱网 | 每 14 格建一组束柱至 `y=42` | 不做屋顶 |
| 6–10 中殿封闭 | 侧廊与拱、逐开间肋拱、高窗、对应飞扶壁、屋脊约 `y=86` 的高坡屋顶 | 每开间做镜像复核和维修通道 |
| 11–14 东西横向及东端 | 耳堂、唱诗班、后殿/环廊、七座放射礼拜堂 | 逐处核验彩窗和可达性 |
| 15–17 双塔上段 | 两塔同步从 `y=32` 至 `96`、`137`、`180` | 实测两塔高度一致 |
| 18–23 细化内部 | 西立面精修、祭坛、管风琴、塔内楼梯与钟楼、家具、隐蔽照明 | 西门通视、全空间可达 |
| 24–27 外围 | 西广场、北修道院、南主教宫、东墓园 | 保留山体和观景视线 |
| 28–30 收尾 | 克制老化、八视角总检、拆临时脚手架和坐标标记 | 最终通道和照明复核 |

**派工粒度**：即使已有批量工具，每次只派一个阶段或一个具体开间；单块工具条件下只派一个可靠可完成的小模块。施工模块完成即更新清单，不把“蓝图已生成”说成“已在世界建成”。

## 4. 世界状态、验收与恢复

重新开始任何施工时先调用 `mc_status`，再用实际可用的 `mc_scan` 或新实现的区域扫描检查本轮目标；如果工具不能看到完整建筑，就说明扫描盲区，并补现场观察或可核验的审计记录。会话记忆只是计划，**世界方块才是完工依据**。遇到玩家正在施工的区域、已存在作品或不可确认覆盖范围时停在该模块，先给出冲突坐标。

每完成一个模块，按优先级审计：**几何 → 承重/支撑 → 对称 → 可达 → 哥特形态 → 细节**。至少检查：`z=0` 主轴；14 格柱距；两塔镜像与同高；扶壁对柱、不撞窗；尖券不是矩形洞；肋拱有真实上升曲线；无悬空大墙；地下、塔楼、屋顶楼梯全部连通；西门到祭坛无遮挡；彩窗分区正确；无超过 9×9 的空白大墙与破坏氛围的裸露光源。重大轴线或承重错误出现时先修复，不能为了保留错误继续扩建。

总检至少从八处观察：西广场中央、西北、西南、北耳堂、南耳堂、东端、中殿西端、圣坛西侧。远处要看到双塔、高耸中殿、十字耳堂、重复飞扶壁和复杂东端，整体不应像方盒子。

### 每轮报告模板

```text
项目：Cathedralis Magna Montis
世界原点与朝向：(X0,Y0,Z0)，相对 x 轴方位
已验证的工具能力：
本轮目标/阶段：
已在世界核实完成的模块与坐标：
本轮实际方块变更数、失败数及审计编号（若可得）：
当前位置（世界坐标及相对坐标）：
轴线/镜像/尺寸/通行检查：
发现的偏移与已修复项：
尚未解决的风险或工具限制：
下一施工模块、起点与需要的材料：
```

### 给游戏内 DeepSeek 的短指令

```text
/msg DeepSeekBot 阅读 building.md，先 mc_status 并扫描当前阶段所在区域，核验世界原点和已建结构。现在只执行 PHASE N 的第一个可完成模块；先报工具能力，施工后按 building.md 的每轮报告模板回复。没有实际可用的批量工具就不要假称批量完成。
```

游戏内私聊未必能自动读取本文件：须先通过当前 DSH 会话可读的方式提供文件内容或明确路径，确认 Agent 已真正读到；不能仅发送文件名就假定它知道施工规范。长任务进度以世界、工具结果和审计记录为准。
