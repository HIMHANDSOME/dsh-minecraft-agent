# 大型建造工程规范（通用模板）

> 交给 DeepSeek Agent 的长期施工文件。项目运行于 README 所述的本机 Minecraft Java 独立服务器；玩家和 `DeepSeekBot` 同场协作。本文的坐标、功能请求和进度状态均须**以游戏世界和实际工具返回为准**，不以对话记忆或"蓝图已生成"为准。
>
> **本文件是通用骨架**：不绑定任何具体建筑形制。请把第 1、2 节的坐标契约、分区与材料换成你自己项目的真实值；第 3、4 节的流程与验收方法可以直接沿用。

---

## 0. 先向 DeepSeek 声明：补足建造能力与游玩便利性

**DeepSeek，请先读本节和项目 README，清点当前真实可用的工具，并报告缺口。**

本仓库的 MCP 工具分两层：

| 层 | 工具 | 能做什么 |
|---|---|---|
| 基础 | `mc_place` / `mc_dig` / `mc_scan` / `mc_collect` … | 单块放置与拆除、按类型与半径检索 |
| 批量（受控） | `mc_fill` / `mc_batch_place` / `mc_scan_region` / `mc_allowed_blocks` | 长方体填充、批量提交、区域实测、材料白名单 |

基础层做不了大工程；批量层已经把大型工程变得可行，但它是**受控**的：坐标必须落在 `build/site.json` 的施工区内，方块必须在材料白名单内，单条体积 ≤ 32768，每一条都读服务端回执并写审计日志。

请先调用 `mc_allowed_blocks` 和 `mc_scan_region` 确认这两层到底能做什么，**不要假定某个工具存在**：工具清单以 `tools/list` 的真实返回为准，不以本文或 README 的描述为准。

如仍缺能力，可按下表向项目维护者申请（需要改代码 + 测试，不是发一句话就能有）：

| 优先级 | 功能 | 建议的受控接口与验收条件 |
| --- | --- | --- |
| P0 | 蓝图与断点 `mc_blueprint` | 导入或生成按阶段、按分区切块的蓝图；预览差异，只修缺失或错误方块；记录每批的世界实际完成状态，失败可续跑 |
| P0 | 回滚与审计 | 每批记录修改前后方块及执行者；支持按批撤销/恢复；`logs/build-audit.jsonl` 可逐条核对 |
| P1 | 复制/镜像 `mc_clone` | 仅在目标范围为空或有明确覆盖许可时复制；先检查镜像轴、方向方块状态、容器与实体；对称结构可按指定轴生成 |
| P1 | 长任务队列与进度 | 分开"提交/进度/暂停/恢复/取消"；分批执行并按区块加载与限速调度；游戏内 `/msg` 回复任务编号与简短进度，防止单轮超时或回复截断造成误判 |
| P1 | 玩家协作便利 | 可按玩家位置设定建造原点；一键导航至阶段入口；临时脚手架与维修通道有明确拆除计划；材料需求清单、储物箱补给与交接报告。保留 `mc_follow` / `mc_goto_player` / `mc_give` |
| P2 | 可选择的施工模式 | 为专用建筑世界提供**受控的**创造模式、材料补给、昼夜/天气设置与安全传送。应由服务端受控接口或人操作，**不意味着**给 Agent 开放任意 `/command`、`/op`、RCON 或外网监听 |

**需要调整的限制**：当前 Agent 的 DSH profile 为 `read-only`，且 Minecraft MCP 不提供任意命令执行。若由维护者升级项目，只给受控建造接口**最小必需权限**；不要直接把文件系统写权限、服务器控制台或重置世界权限交给游戏内聊天。`run-server.sh` / `run-server.ps1` 的 `cmd` 是**人工运维入口**，不等同于 Agent 的 MCP 能力。单条游戏内任务默认队列、超时和回复长度见 README；长工程应改为可恢复的后台任务。daemon 与一次性 `run-agent` 使用同名机器人，不要同时启动。

**开工门槛**：先报告"已实现并验证 / 尚未实现"的能力清单；如果能力未升级，仍可只用现有工具建造一个**可实际核验的小模块**，并如实记录进度。**不得虚构** `fill`、`clone` 或已建结构。整个工程只动此专用服务器世界，不重置、不覆盖既有作品，除非玩家另行明确指定。

---

## 1. 项目目标与坐标契约

<!-- ↓↓↓ 换成你自己的项目 ↓↓↓ -->

**项目目标**：用一句话写清要建什么、给谁用、验收时"看到什么算完成"。

**本项目规模**：约 `__ × __ × __` 格（长 × 宽 × 高）。请在动工前把这个数字写死，后面所有工具的超时与分批策略都按它推算。
如果结构高度超过 `mc_fill` 单条 32768 格的体积上限（例如长宽各 40 格、高 21 格就到了 33600），就必须按层或按分区切块提交，`mc_batch_place` 的 `ops` 单次上限是 500 条。

<!-- ↑↑↑ 换成你自己的项目 ↑↑↑ -->

### 1.1 坐标契约

- 世界原点 `O=(X0,Y0,Z0)`：由 PHASE 0 实测后**锁定**，并写进 `build/site.json` 的 `origin`。
- 本文 `x,y,z` 均为**相对坐标**；写入世界时 `X=X0+x`、`Y=Y0+y`、`Z=Z0+z`。
- 约定方向必须在动工前写清楚（例如 `x` 增大指向哪个方位、`z<0` 是北还是南）。**若实际世界方位不符合设计，先记录旋转变换**，不可把相对坐标直接当世界坐标下发。
- 测量世界可建上下界（`build/site.json` 的 `worldHeight`），顶部留至少 8 格余量。
- 坐标是**设计控制值**。地基、门洞、周边道路需要细化时记录变更；**主体轴线、开间模数、总高和中央通视必须保持一致**。

### 1.2 平面分区

| 区域 | 相对范围 | 核心内容 |
| --- | --- | --- |
| A | `x=…` | … |
| B | `x=…` | … |
| C | `x=…` | … |

<!-- 分区表的实际作用：让每个施工阶段有明确的地理边界，避免"一边建一边改轴线"。 -->

**尺寸冲突必须在 PHASE 1 冻结前解决**。同一批数据里出现两个互相矛盾的尺寸（例如"净宽 29 格"与"柱中心 ±15、柱宽 7 格"）时，选一套并冻结；**绝不在建造中途混用两套坐标**。

---

## 2. 地形、建造语言与材料

### 地形与台地

- 先勘察：本项目主体需要多大的连续可建面？东西/南北各需多少格？视线通廊朝向哪边？
- 尽量保留地形。需要整平时设明确的台地标高；**山坡悬空处必须建实际基础，不允许主体漂浮**。
- 外围（广场、道路、围墙、绿化）要与主体风格一致，且不要遮挡主要观景视线。

### 结构与垂直剖面

- 写清主要承重构件的**视觉尺度**与做法（例如柱子最小截面、必须有三段式柱基与柱头，禁止用单根直柱直接顶到屋面）。
- 给出垂直剖面控制标高（相对地坪）：基础顶 / 各层楼板 / 墙顶 / 屋面起坡 / 屋脊。**用现场剖面校核**，不要把"净宽"和"柱中心距"混同。
- 若结构带有曲线或斜面，必须保证其**真实升起**，不允许用平顶贴假造型。
- 大跨度屋顶要有可见的支撑逻辑（柱、梁、拱、斜撑任选），不允许出现无支撑的大面积悬空墙体。

### 采光、内部与可达性

- 明确主要出入口位置、尺寸与开启方向。
- 明确每个窗的位置与尺寸，以及是否有窗框/窗格；**不允许只放一块矩形玻璃**充当造型窗。
- **所有主要空间必须真实可进入**：地面、地下、楼层之间、屋顶维护通道都要有连通的楼梯或通道。这是验收硬指标，不是加分项。
- 内部家具（座椅、储物、照明、工作台等）在结构封顶后再做。

### 材料规范

<!-- ↓↓↓ 换成你自己的材料表 ↓↓↓ -->

| 用途 | 主要材料 |
| --- | --- |
| 主结构 | `stone_bricks`；局部 `andesite`、`polished_andesite` |
| 造型高光 | `polished_andesite`、`stone_bricks`、少量 `calcite` |
| 墙体老化与阴影 | 少量 `cracked_stone_bricks`、`mossy_stone_bricks`；`tuff_bricks`、`polished_tuff`、`deepslate_bricks` |
| 屋面与木作 | `deepslate_tiles`（该版本若无此方块，先映射到可用的深板岩楼梯/台阶）、`polished_deepslate`；`dark_oak`、`spruce` |
| 金属与照明 | `iron_bars`、`iron_chain`、`lantern`、少量 `soul_lantern`；避免大量裸露萤石和火把 |

<!-- ↑↑↑ 换成你自己的材料表 ↑↑↑ -->

大面积墙体的材质参考比例（例如 55% 主石 / 15% 次石 / 10% 磨制 / 8% 其他砖 / 5% 老化 / 3% 高光 / 4% 其他）**仅用于墙面视觉分布**；**柱、梁、窗框和承重点必须有固定结构规律**，不可随机噪声化。

施工前核对本版本真实的方块 ID：`mc_allowed_blocks` 返回的就是可以下发的清单。不在清单内的名称**不得直接下发**；需要新方块时先在 `build/site.json` 的 `extraAllowedBlocks` 里加上，或改 `bot/builder-core.js` 的默认白名单。

---

## 3. 分阶段施工路线

每阶段按"测量 → 定位 → 骨架 → 墙体 → 柱/梁 → 顶部 → 支撑件 → 窗/门 → 内部 → 装饰 → 灯光 → 家具 → 检查"中对应的步骤推进。**完成一个模块后立刻核对坐标、尺寸、中轴线、镜像与通行**；发现基础错误先返修，不要带着错误继续往上建。

| 阶段 | 本阶段可交付结果 | 停止点 |
| --- | --- | --- |
| 0 勘察 | 选原点 `(X0,Y0,Z0)`、测世界高度、地形与可扩建范围，写入 `build/site.json` | 仅报告，不建主体 |
| 1 测量 | 定各分区边界线；中轴、柱线、外墙、支撑件位置的临时标记 | **冻结**统一网格与旋转变换 |
| 2 地基 | 各分区地基，深到实土并支撑坡面 | 无悬空结构 |
| 3 地下 | 地下结构（若有）：墙、柱、顶、至少两条通地面楼梯 | 先不做大量装饰 |
| 4 主体下部 | 出入口、底层墙体、主要承重柱 | 对称构件同步建 |
| 5 主体骨架 | 柱/梁到设计标高 | 不做屋顶 |
| 6–10 主体封闭 | 墙体、顶部结构、高窗、外部支撑件、屋面 | 每段做镜像复核与维修通道 |
| 11–14 横向/附属体 | 附属空间、侧翼、特殊造型段 | 逐处核验采光与可达性 |
| 15–17 高耸构件 | 塔/烟囱/高墙等，同步分层上升 | **实测各构件高度一致** |
| 18–23 内部细化 | 入口精修、主要陈设、楼梯与平台、家具、隐蔽照明 | 主视线通视、全空间可达 |
| 24–27 外围 | 广场、道路、围墙、绿化、附属建筑 | 保留地形与观景视线 |
| 28–30 收尾 | 克制的老化处理、八视角总检、拆除临时脚手架与坐标标记 | 最终通道与照明复核 |

**派工粒度**：即使已有批量工具，**每次只派一个阶段或一个具体分区**。施工模块完成即更新清单；**不把"蓝图已生成"说成"已在世界建成"**。

---

## 4. 世界状态、验收与恢复

重新开始任何建造时，先调用 `mc_status`，再用 `mc_scan_region`（或 `mc_scan`）检查本轮目标区域；**如果工具看不到完整结构，就说明扫描盲区**，并补现场观察或可核验的审计记录。会话记忆只是计划，**世界方块才是完工依据**。

遇到玩家正在施工的区域、已存在作品或不可确认的覆盖范围时，**停在该模块**，先给出冲突坐标，不要自行决定覆盖。

### 每完成一个模块的审计顺序

**几何 → 承重/支撑 → 对称 → 可达 → 造型 → 细节**

至少检查：

- 中轴线是否笔直、是否有构件横跨中央通视带；
- 开间模数与柱线是否与第 1 节冻结值一致；
- 对称构件是否镜像且等高；
- 外部支撑件是否对准内部承重点、是否撞窗；
- 有曲线的造型是否真实升起，而不是平顶贴假；
- **有没有悬空大墙**；
- 地下、楼层、屋顶的楼梯是否全部连通；
- 主入口到视觉焦点是否无遮挡；
- 窗的位置与配色分区是否正确；
- 有没有超过 9×9 的空白大墙，以及破坏氛围的裸露光源。

**重大轴线或承重错误出现时先修复**，不能为了保留错误继续扩建。

### 总检视角

至少从八处观察：正面主入口、左前、右前、左侧、右侧、背面、室内主视点、最高点。远处应能看到设计中的体量关系与重复构件；**整体不应像一个方盒子**。

### 每轮报告模板

```text
项目：<项目名>
世界原点与朝向：(X0,Y0,Z0)，相对 x 轴方位
已验证的工具能力：
本轮目标/阶段：
已在世界核实完成的模块与坐标：
本轮实际方块变更数、失败数及审计编号（若可得）：
当前位置（世界坐标及相对坐标）：
轴线/镜像/尺寸/通行检查：
发现的偏移与已修复项：
尚未解决的风险或工具限制：
下一建造模块、起点与需要的材料：
```

### 给游戏内 DeepSeek 的短指令

```text
/msg DeepSeekBot 阅读 building.md，先 mc_status 并扫描当前阶段所在区域，核验世界原点和已建结构。现在只执行 PHASE N 的第一个可完成模块；先报告真实可用的工具，建造后按 building.md 的每轮报告模板回复。没有实际可用的批量工具就不要假称批量完成。
```

游戏内私聊**未必能自动读取本文件**：须先通过当前 DSH 会话可读的方式提供文件内容或明确路径，并确认 Agent 已真正读到；不能仅发送文件名就假定它知道施工规范。长任务进度**以世界、工具结果和审计记录为准**。

---

## 5. 审计与回滚

批量建造的每一次写入都会记进 `logs/build-audit.jsonl`（服务端侧，模型无法伪造）：

| 字段 | 含义 |
| --- | --- |
| `kind` | `fill` / `setblock` |
| `cmd` | 实际下发的 `/fill` 或 `/setblock` 指令 |
| `box` / `block` / `mode` / `vol` | 目标框、方块、模式、体积 |
| `ack` | 服务端原样回执（`Successfully filled N block(s)` / `No blocks were filled` / 报错原文） |
| `ok` / `filled` / `noop` | 判定结果；`noop` = 指令成功但 0 改动（幂等重跑的正常情形） |

`mc_fill` 支持 `dryRun: true`，只校验不写入，适合在真正施工前核对坐标与体积。

回滚用 `bot/builder-core.js` 的 `snapshotRegion()` + `rollbackCommands()`：写入前对目标框做调色板压缩快照，可生成反向 `/fill` 序列。快照体积超限时会被拒绝（可用 `allowNoSnapshot` 显式放行），此时该批次将**没有**可用的反向序列——请自己确认是否接受。
